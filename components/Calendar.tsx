"use client"

//TODO: add tasks to calendar and make it possible to click on day and see tasks for that day

import { DayPicker } from 'react-day-picker';
import 'react-day-picker/dist/style.css';

export default function Calendar() {
    return (
        <div className=''>
            {/* <DayPicker modifiers={{hasTask: tasks.map(t => new Date(t.deadline))}} modifiersClassNames={{hasTask: "bg-red-400 text-white"}}/> */}
        </div>
    );
}